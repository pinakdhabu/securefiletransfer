# SecureFileTransfer

A minimal Qt6 C++ example demonstrating a GUI client/server file transfer with a simple XOR encryption.

Build (out-of-source):

```bash
mkdir build && cd build
cmake ..
cmake --build .
```

Run the application. Choose Client or Server mode in the GUI.
